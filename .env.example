# Copy this file to .env (same folder as app.py) and fill in your real key.
# .env is loaded automatically by app.py via python-dotenv — never commit
# the real .env file or paste your key into chat, source control, or Slack.
ANTHROPIC_API_KEY=sk-ant-api03-your-key-here
